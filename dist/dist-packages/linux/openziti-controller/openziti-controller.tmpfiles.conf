# Ensure state directory ownership matches the static service user.
# This is a safety net for the DynamicUser -> static user migration.
# See tmpfiles.d(5) for syntax details.
Z /var/lib/ziti-controller 0700 ziti-controller ziti-controller - -

[Service]

#
## Optional Permissions
#

# allow binding low ports, e.g., 443/tcp; NOTE: use TLS passthrough if fronting with a reverse proxy, i.e., "raw" TCP
# proxy
# AmbientCapabilities=CAP_NET_BIND_SERVICE

#
## Optional Parameters
#
# you must re-initialize with an empty ExecStart value before setting a new start command if you require a non-standard
# command line
# ExecStart=
# ExecStart=/opt/openziti/bin/ziti controller run config.yml --verbose --log-formatter pfxlog

## ziti fabric stream events

Stream events

```
ziti fabric stream events [flags]
```

### Examples

```
ziti fabric stream events --circuits --metrics --metrics-filter '.*'
```

### Options

```
      --all                               Include all events
      --api-sessions                      Include api-session events
      --circuits                          Include circuit events
  -i, --cli-identity string               Specify the saved identity you want the CLI to use when connect to the controller with
      --entity-change                     Include entity change events
      --entity-counts                     Include entity count events
      --entity-counts-interval duration   Specify the entity count event interval (default 5m0s)
  -h, --help                              help for events
      --links                             Include link events
      --metrics                           Include metrics events
      --metrics-filter string             Specify which metrics to stream
      --metrics-source-filter string      Specify which sources to stream metrics from
  -j, --output-json                       Output the full JSON response from the Ziti Edge Controller
      --output-request-json               Output the full JSON request to the Ziti Edge Controller
      --routers                           Include router events
      --services                          Include service events
      --sessions                          Include session events
      --terminators                       Include terminators events
      --timeout int                       Timeout for REST operations (specified in seconds) (default 5)
      --usage                             Include usage events
      --usage-version uint8               Specify which version of usage data to stream. Valid versions: [2,3] (default 3)
      --verbose                           Enable verbose logging
```

### SEE ALSO

* [ziti fabric stream](../stream.md)	 - stream fabric operational data

###### Auto generated by spf13/cobra on 26-Aug-2024

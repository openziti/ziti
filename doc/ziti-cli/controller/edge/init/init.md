## ziti controller edge init



```
ziti controller edge init <config> [-p] [flags]
```

### Examples

```
ziti controller edge init controller.yml -u admin -p o93wjh5n
```

### Options

```
  -h, --help              help for init
  -n, --name string       the admin display name to initialize with, defaults to 'Default Admin' (default "Default Admin")
  -p, --password string   the admin password value to initialize with, prompted for if not supplied
  -u, --username string   the admin username value to initialize with, prompted for if not supplied
```

### Options inherited from parent commands

```
      --cli-agent-addr string    Specify where CLI Agent should list (ex: unix:/tmp/myfile.sock or tcp:127.0.0.1:10001)
      --cli-agent-alias string   Alias which can be used by ziti agent commands to find this instance
  -a, --cliagent                 Enable/disabled CLI Agent (enabled by default) (default true)
      --log-formatter string     Specify log formatter [json|pfxlog|text]
  -v, --verbose                  Enable verbose logging
```

### SEE ALSO

* [ziti controller edge](../edge.md)	 - used to perform various edge related functionality

###### Auto generated by spf13/cobra on 26-Aug-2024

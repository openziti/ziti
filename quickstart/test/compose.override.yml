services:
  quickstart-test:
    image: golang:${ZITI_GO_VERSION:-noop}-alpine
    volumes:
      - .:/mnt
    working_dir: /mnt
    environment:
      - ZITI_PWD
    networks:
      - ziti
    command: go test -v ./quickstart_test.go
